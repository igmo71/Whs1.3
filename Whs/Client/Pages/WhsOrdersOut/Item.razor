@page "/WhsOrdersOut/Item/{Id}"
@page "/WhsOrdersOut/Item/{Id}/{SearchStatus}"
@using Whs.Client.Components

<Notification @ref="Notification" />

<input type="hidden" id="Barcode" value="@Barcode" @onchange="ScannedBarcodeAsync" />

@if (OrderDto == null)
{
    <h3>Расходный ордер на товары</h3>
    <p><em>Загрузка...</em></p>
}
else
{
    <div class="row">
        <div class="col-2 d-print-none">
            <button class="btn btn-primary" @onclick="Return"><span class="oi oi-action-undo"></span> Назад </button>
        </div>
        <div class="col-6">
            <div class="h3">
                @OrderDto.Item?.Документ_Name
            </div>
            <div class="align-middle text-center border border-dark">
                @OrderDto.Item?.Статус
            </div>
        </div>
        <div class="col-4 d-none d-print-inline">
            <div class="float-right">
                <img src=@($"data:image/jpeg;base64,{OrderDto.BarcodeBase64}") alt="штрих код" width="300" height="80" />
            </div>
        </div>
        <div class="col-2 mb-2 d-print-none">
            <button class="btn btn-info" @onclick="PrintAsync"><span class="oi oi-print"></span> Печать </button>
        </div>
        <div class="col-2 wh-label">
            <div class="display-4 align-middle text-center border border-dark">
                @OrderDto.Item?.НомерОчереди
            </div>
            <div class="align-middle text-center border border-dark">
                @(OrderDto.Item.Оплачено ? "оплачено" : "НЕ оплачено")
            </div>
        </div>
    </div>

    <table>
        <tr>
            <th>Склад: </th>
            <td>@OrderDto.Item?.Склад_Name</td>
            <th>Тип очереди:</th>
            <td>@OrderDto.Item?.ТипОчереди</td>
        </tr>
        <tr>
            <th>Получатель: </th>
            <td>@OrderDto.Item?.ОтправительПолучатель_Name</td>
            <th>Выполнить до: </th>
            <td>@OrderDto.Item?.СрокВыполненияString</td>
        </tr>
        <tr>
            <th>Распоряжение: </th>
            <td>
                @foreach (var item in OrderDto.Item?.Распоряжения)
                {
                    @item.Распоряжение_Name <br />
                }
            </td>
            <th>В работе: </th>
            <td></td>
        </tr>
        <tr>
            <th>Ответственный: </th>
            <td>@OrderDto.Item?.Ответственный_Name</td>
            <th>Направление: </th>
            <td>@OrderDto.Item?.НаправлениеДоставки_Name</td>
        </tr>
        <tr>
            <th>Комментарий: </th>
            <td>@OrderDto.Item?.Комментарий</td>
            <th>Вес: </th>
            <td>@OrderDto.Item?.Вес</td>
        </tr>
    </table>


    <table class="table">
        <thead>
            <tr>
                <th class="wh-th">№</th>
                <th class="wh-th">Артикул</th>
                <th class="wh-th">Товар</th>
                <th class="wh-th">Кол план</th>
                @if (OrderDto.Item?.Статус != "Подготовлено")
                {
                    <th class="wh-th d-print-none">Причина изменения</th>
                    <th class="wh-th d-print-none">Кол факт</th>
                    <th class="wh-th d-none d-print-table-cell">Кол факт</th>
                }
                <th class="wh-th">Упак</th>
                <th class="wh-th">Вес</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in OrderDto.Item.Товары)
            {
                <tr>
                    <td class="wh-td-col-1">@item.НомерСтроки</td>
                    <td class="wh-td-col-2">@item.Артикул</td>
                    <td class="wh-td-col-3">@item.Номенклатура_Name</td>
                    <td class="wh-td-col-4">@item.КоличествоПлан</td>

                    @if (OrderDto.Item?.Статус != "Подготовлено")
                    {
                        <td class="wh-td-col-5 d-print-none">
                            <select class="form-control" @bind="item.Data.EditingCauseId">
                                <option value="@Guid.Empty" selected></option>
                                @foreach (var cause in EditingCauses)
                                {
                                    <option value="@cause.Id">@cause.Name</option>
                                }
                            </select>
                        </td>
                        <td class="wh-td-col-6 d-print-none">
                            <input class="form-control" @bind="item.КоличествоФакт" />
                            <div class="input-group-append">
                                <button class="btn btn-outline-secondary" type="button" >&times;</button>
                            </div>
                        </td>
                        <td class="wh-td-col-6 d-none d-print-table-cell">
                            &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;
                        </td>
                    }
                    @if (OrderDto.Item?.Статус == "Подготовлено")
                    {
                        <td class="wh-td-col-5">@item.Data?.EditingCause.Name</td>
                        <td class="wh-td-col-6 d-print-none">@item.КоличествоФакт</td>
                    }
                    <td class="wh-td-col-7">@item.Упаковка_Name</td>
                    <td class="wh-td-col-8">@item.Вес</td>
                </tr>
            }
        </tbody>
    </table>
}
